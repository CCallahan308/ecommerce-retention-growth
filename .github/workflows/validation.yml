name: Validation Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          
      - name: Install dependencies
        run: |
          pip install -r requirements-dev.txt || pip install pytest ruff jupyter tqdm
          pip install -e .

      - name: Lint Code
        run: make lint

      - name: Generate Mock Data
        run: make data

      - name: Execute PyTest Suite
        run: make test
